[project]
name = "shared"
version = "0.1.0"
dependencies = [
    "alembic>=1.18.4",
    "asyncpg>=0.31.0",
    "sqlalchemy>=2.0.49",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["database", "alembic"]

